{{- if eq .Values.frontend.autoscaling.mode "keda" }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: pos-frontend-scaledobject
spec:
  scaleTargetRef:
    name: pos-frontend
  minReplicaCount: {{ .Values.frontend.autoscaling.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.frontend.autoscaling.keda.maxReplicaCount }}
  triggers:
    - type: cron
      metadata:
        timezone: {{ .Values.frontend.autoscaling.keda.timezone | default "UTC" | quote }}
        start: {{ .Values.frontend.autoscaling.keda.start | default "0 22 * * *" | quote }}
        end: {{ .Values.frontend.autoscaling.keda.end | default "0 8 * * *" | quote }}
        desiredReplicas: {{ .Values.frontend.autoscaling.keda.desiredReplicas | default "0" | quote }}
    - type: cpu
      metadata:
        type: Utilization
        value: "{{ .Values.frontend.autoscaling.keda.cpuAverageUtilization }}"
    - type: memory
      metadata:
        type: Utilization
        value: "{{ .Values.frontend.autoscaling.keda.memoryAverageUtilization }}"
{{- end }}