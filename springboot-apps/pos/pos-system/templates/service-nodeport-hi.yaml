apiVersion: v1
kind: Service
metadata:
  name: pos-backend-service-nodeport
  namespace: pos-prod
spec:
  type: NodePort
  selector:
    app: pos-backend
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30007