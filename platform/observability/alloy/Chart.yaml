apiVersion: v2
name: alloy
description: Grafana Alloy configuration for POS application log collection
type: application
version: 0.1.0
appVersion: "1.0"

# This is a wrapper chart that depends on the official Grafana Alloy Helm chart
dependencies:
  - name: alloy
    version: "0.12.*"
    repository: https://grafana.github.io/helm-charts
