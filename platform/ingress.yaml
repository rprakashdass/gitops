apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-ingress-subdomains
  namespace: monitoring
spec:
  ingressClassName: traefik
  rules:
    - host: grafana.rprakashdass.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 80

    - host: loki.rprakashdass.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki-gateway
                port:
                  number: 80

    - host: mimir.rprakashdass.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mimir-nginx
                port:
                  number: 80

    - host: minio.rprakashdass.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio-console
                port:
                  number: 9001
