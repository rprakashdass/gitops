apiVersion: v1
kind: ConfigMap
metadata:
  name: rule-kube-deployment-replicas-mismatch
  namespace: monitoring
  labels:
    app: mimir-rules
    env: production
data:
  kube-deployment-replicas-mismatch.yaml: |
    groups:
    - name: kube-deployment-replicas-mismatch
      rules:
      - alert: KubeDeploymentReplicasMismatch
        expr: kube_deployment_spec_replicas != kube_deployment_status_replicas_available
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Deployment {{ $labels.deployment }} in namespace {{ $labels.namespace }} has a replica mismatch"
          description: "The number of available replicas for deployment {{ $labels.deployment }} does not match the desired number."
